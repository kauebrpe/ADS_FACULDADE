//1. Escreva um programa que imprima os números de 1 a 10 usando while.
#include <stdio.h>
int main() {
    int numero = 1;
    while (numero <= 10) {
        printf("%d ", numero);
        numero = numero + 1;
    }
    return 0;
}
