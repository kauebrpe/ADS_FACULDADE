#include <stdio.h>
int main() {
   int id, cont = 1; // cont= quantas repetições vou fazer
   while(cont <= 5){
      printf("Digite idade: ");
      scanf("%i", &id);
      cont ++; // ou (cont = cont + 1;)
   }
    return 0;
}

